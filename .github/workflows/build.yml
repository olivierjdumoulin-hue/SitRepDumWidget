name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Build
      run: |
        echo "SitRepDum Widget Build"

    - name: Upload
      uses: actions/upload-artifact@v4
      with:
        name: SitRepDumWidget
        path: .
